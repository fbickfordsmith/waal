# Channels are checked in the order they are listed.

name: waal

channels:
  - pytorch
  - conda-forge
  - nvidia

dependencies:
  - cudatoolkit
  - matplotlib
  - numpy
  - pandas
  - pip
  - python=3.10
  - pytorch
  - pytorch-cuda=11.8
  - torchvision